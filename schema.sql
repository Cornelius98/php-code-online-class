--- Creating a database
CREATE DATABASE `scratch_db`;

--- Creating a table
CREATE TABLE `person`(
   `name` VARCHAR(30) NOT NULL,
   `height` VARCHAR(3) NOT NULL,
   `city` VARCHAR(30) NOT NULL
);